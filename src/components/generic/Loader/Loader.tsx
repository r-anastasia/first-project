// local libs
import { LoaderContainer, LoaderTitle } from './styles'

export const Loader = () => {
  return (
    <LoaderContainer>
      <LoaderTitle>Loading</LoaderTitle>
    </LoaderContainer>
  )
}
